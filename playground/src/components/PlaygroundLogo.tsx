import { Gamepad2 } from "lucide-react"

export function PlaygroundLogo({ className }: { className?: string }) {
  return (
    <Gamepad2
      className={className}
      style={{
        color: "#10b981",
      }}
    />
  )
}
